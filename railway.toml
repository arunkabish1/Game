[build]
builder = "NIXPACKS"

[build.nixpacks]
packages = ["nodejs", "npm"]

[phases.install]
cmd = "cd backend && npm ci"

[phases.build]
cmd = "echo 'Build complete - dependencies installed'"

[start]
cmd = "cd backend && node server.js"
