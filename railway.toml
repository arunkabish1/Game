[build]
builder = "NIXPACKS"

[build.nixpacks]
packages = ["nodejs", "npm"]

[phases.install]
cmd = "cd backend && npm ci"

[phases.build]
cmd = "cd backend && npm run build || echo 'Build phase complete'"

[start]
cmd = "cd backend && node server.js"
