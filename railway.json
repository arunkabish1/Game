{
  "build": {
    "builder": "NIXPACKS",
    "nixpacksPlan": {
      "phases": {
        "install": {
          "cmds": ["npm install --prefix backend"]
        },
        "build": {
          "cmds": []
        },
        "start": {
          "cmd": "npm start --prefix backend"
        }
      }
    }
  }
}
